[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

# Package metadata lives in setup.cfg (not the PEP 621 [project] table here)
# so that older pip/setuptools found on long-lived VPN servers can still
# build a working package instead of an empty "UNKNOWN-0.0.0" wheel.
